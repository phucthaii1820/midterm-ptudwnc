export interface ModalUpdatePresentationProps {
  namePresentation: string
  // eslint-disable-next-line no-unused-vars
  handleUpdatePresentation: (namePresentation: string) => void
  isDelete: boolean
  fetchData: () => void
}
